[tox]
envlist = py311,lint,type

[testenv]
deps = .[dev,agents]
commands = pytest --cov=src --cov-report=term-missing --cov-fail-under=100

[testenv:lint]
deps = .[dev]
commands =
    ruff format --check .
    ruff check .

[testenv:type]
deps = .[dev]
commands = mypy src
